{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "build-e": "pulp build -O --to html/index.js",
    "example": "pulp build -O --to html/index.js && firefox html/index.html"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript": "^0.11.7",
    "purescript-psa": "^0.5.0",
    "rimraf": "^2.5.4"
  }
}
